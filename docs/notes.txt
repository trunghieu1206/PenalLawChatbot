- in the response returned by AI, some of the law articles still include "BLHS", which makes the system unable to query, deal with this completely
- also there is a bug that for different source, the same article number might leads to a completely different content. For example article number 249 in BLHS 2025 is about drugs, but article number 249 in BLHS BLHS 2009 is about gambling. Think of a logic to address this issue 
- in the study mode, for the suggested laws advised by the AI, make it so that when user clicks onto it the original law is shown as a reference. Also in this window, the suggested laws again include the word "BLSH" which makes user unable to understand, please add the full source (for e.g: "BLHS 2009"). 
- in the system (whole project) remove the keyword "BLHS", and replace with the full "Bộ luật Hình sự" so that it is more understandable for users