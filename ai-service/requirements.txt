fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-dotenv==1.0.1
torch>=2.1.0
# sentence-transformers must match the version that saved the embedding model (3.3.1).
# bge-reranker-v2-m3 also requires FlagEmbedding which in turn needs >=3.x.
sentence-transformers>=3.3.0
FlagEmbedding>=1.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.7
langchain-milvus>=0.1.0
langgraph>=0.1.0
huggingface_hub>=0.23.0
pymilvus>=2.4.0
# milvus-lite <2.4.9 uses `from pkg_resources import ...` which fails when
# setuptools is in a non-standard location. >=2.4.9 switched to importlib.metadata.
milvus-lite>=2.4.9
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
transformers>=4.44.0
accelerate>=0.30.0
# peft>=0.18.0 required:
#   - 0.15.x introduced corda_config support needed to load the adapter
#   - 0.18.x fixed the broken `EncoderDecoderCache` import from transformers
#     (that class was removed in transformers >=4.47)
# Always pin peft and transformers together — they must be compatible.
peft>=0.18.0
# setuptools provides pkg_resources, which milvus-lite imports at module load.
# Without it, MilvusClient raises "No module named 'pkg_resources'".
setuptools>=70.0
